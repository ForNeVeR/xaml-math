<?xml version="1.0" encoding="utf-8" ?>
<UserControl
	x:Class="WinUIMath.Example.Views.FormulaPresetView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="using:WinUIMath.Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="using:WinUIMath.Example.Views"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="1*" />
			<RowDefinition Height="auto" />
			<RowDefinition Height="1*" />
		</Grid.RowDefinitions>

		<ScrollViewer
			Grid.Row="0"
			HorizontalScrollBarVisibility="Auto"
			VerticalScrollBarVisibility="Auto">
			<controls:FormulaControl
				x:Name="PART_FormulaControl"
				HorizontalAlignment="Left"
				VerticalAlignment="Top"
				FontSize="24"
				Formula="{x:Bind Preset.Formula, Mode=OneWay}" />
		</ScrollViewer>

		<Slider
			Grid.Row="1"
			Header="FormulaControl.FontSize"
			Maximum="64"
			Minimum="8"
			ValueChanged="OnSliderValueChanged"
			Value="20" />

		<TextBox
			Grid.Row="2"
			HorizontalAlignment="Stretch"
			VerticalAlignment="Stretch"
			AcceptsReturn="True"
			FontFamily="Consolas"
			Text="{x:Bind Preset.Formula, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
			TextWrapping="Wrap" />
	</Grid>

</UserControl>
